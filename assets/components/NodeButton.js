import { View, Text, Pressable } from "react-native";

export default function NodeButton({nodeNumber}) {
    <View>
        <Pressable>
            <Text>Node {nodeNumber}</Text>
        </Pressable>
    </View>
}